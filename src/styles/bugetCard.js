import styled from "styled-components";

export const AllValuesCard = styled.div`

    border: 1px solid var(--color-grey-2);

    padding: 2rem;

    max-width: 490px;
    width: 100%;
    height: 8rem;

    margin-top: 2rem;


    gap: 0.5rem;

    h3 {
        display: flex;
        justify-content: space-between;

        font-size: 1rem;
        font-weight: 700;
        color: var(--color-grey-4);
    }


    span {

        font-size: 1rem;
        font-weight: 700;
        color: var(--color-primary);
    }

    p {
        font-size: 0.75rem;
        font-weight: 400;
    }
    
`